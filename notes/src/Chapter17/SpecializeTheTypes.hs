module Chapter17.SpecializeTheTypes where

-- 1.
-- Specialize for []

-- Methods
-- pure :: a -> ? a
-- (<*>) :: ? (a -> b) -> ? a -> ? b


-- 2.
-- IO

-- Methods
-- pure :: a -> ? a
-- (<*>) :: ? (a -> b) -> ? a -> ? b


-- 3.
-- (,) a

-- Methods
-- pure :: a -> ? a
-- (<*>) :: ? (a -> b) -> ? a -> ? b


-- 4.
-- (->) e

-- Methods
-- pure :: a -> ? a
-- (<*>) :: ? (a -> b) -> ? a -> ? b